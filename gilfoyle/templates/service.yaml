apiVersion: v1
kind: Service
metadata:
  name: {{ include "gilfoyle.fullname" . }}
  labels:
    {{- include "gilfoyle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gilfoyle.selectorLabels" . | nindent 4 }}
